require 'rails_helper'

describe CategoriesController do
 # describe 'routing' do
 #   it "routes to #show" do
 #     expect(get("/:slug")).to route_to("categories#show", :slug  => :slug")
 #    end
 #  end
end
