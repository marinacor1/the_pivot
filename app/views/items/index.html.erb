<h1>All Items</h1>

<% @items.each do |item| %>
  <div class="index-items">
    <%= item.name %>
    <%= button_to "I'm interested", carts_path(item_id: item.id) %>
    <img src="<%= item.image_url %>">
  </div>
<% end %>
