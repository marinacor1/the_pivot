    <nav class="red lighten-1">
      <div class="container">
        <div class="center-align">
          <% @nav_categories.each do |category| %>
            <%= link_to category.name, category_path(category.id), class: "nav-category-links" %>
            
          <% end %>
        </div>
      </div>
    </nav>
